<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="hr_salary_advance_form" model="ir.ui.view">
        <field name="name">hr.salary.advance.form</field>
        <field name="model">hr.salary.advance</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_confirm" string="Confirmer" type="object"
                            invisible="state != 'draft'" class="btn-primary"/>
                    <button name="action_approve" string="Approuver" type="object"
                            invisible="state != 'confirmed'" class="btn-primary"/>
                    <button name="action_cancel" string="Annuler" type="object"
                            invisible="state in ('done', 'cancelled')"/>
                    <button name="action_reset_draft" string="Remettre en brouillon" type="object"
                            invisible="state not in ('confirmed', 'cancelled')"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,approved,done"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="employee_id" readonly="state != 'draft'"/>
                            <field name="department_id"/>
                            <field name="date" readonly="state != 'draft'"/>
                        </group>
                        <group>
                            <field name="amount" readonly="state not in ('draft', 'confirmed')"/>
                            <field name="nb_mensualites" readonly="state not in ('draft', 'confirmed')"/>
                            <field name="montant_mensuel"/>
                            <field name="currency_id" invisible="1"/>
                        </group>
                    </group>
                    <group>
                        <group>
                            <field name="montant_rembourse"/>
                            <field name="solde_restant"/>
                        </group>
                        <group>
                            <field name="reason" readonly="state not in ('draft', 'confirmed')"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Échéances de remboursement" invisible="state == 'draft'">
                            <field name="line_ids" readonly="1">
                                <tree>
                                    <field name="sequence" string="N°"/>
                                    <field name="date"/>
                                    <field name="amount"/>
                                    <field name="is_paid" widget="boolean_toggle"/>
                                    <field name="payslip_id"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="hr_salary_advance_tree" model="ir.ui.view">
        <field name="name">hr.salary.advance.tree</field>
        <field name="model">hr.salary.advance</field>
        <field name="arch" type="xml">
            <tree decoration-info="state == 'draft'"
                  decoration-warning="state == 'confirmed'"
                  decoration-success="state == 'approved'"
                  decoration-muted="state in ('done', 'cancelled')">
                <field name="name"/>
                <field name="employee_id"/>
                <field name="date"/>
                <field name="amount"/>
                <field name="nb_mensualites"/>
                <field name="solde_restant"/>
                <field name="state" widget="badge"
                       decoration-info="state == 'draft'"
                       decoration-warning="state == 'confirmed'"
                       decoration-success="state == 'approved'"/>
            </tree>
        </field>
    </record>

    <record id="hr_salary_advance_action" model="ir.actions.act_window">
        <field name="name">Avances sur Salaire</field>
        <field name="res_model">hr.salary.advance</field>
        <field name="view_mode">tree,form</field>
    </record>

    <menuitem id="menu_hr_salary_advance"
              name="Avances sur Salaire"
              parent="payroll.payroll_menu_root"
              action="hr_salary_advance_action"
              sequence="40"/>

</odoo>
